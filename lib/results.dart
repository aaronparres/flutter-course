import 'package:flutter/material.dart';

class Results extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('No more questions!!!', style: TextStyle(fontSize: 22));
  }
}
